
{# Pagination Controls #}
<div class="pagination__wrap">
    <ul class="list-wrap">
        {# Bouton "Previous" #}
        {% if pagination.has_previous_page %}
            {% if route == 'blog_index_fr' %}
                <li><a href="{{ path('blog_indexfr2', { 'page': pagination.previous_page }) }}"><i class="fas fa-angle-double-left"></i></a></li>
            {% elseif route == 'blog_indexfr2' %}
                <li><a href="{{ path('blog_indexfr2', { 'page': pagination.previous_page }) }}"><i class="fas fa-angle-double-left"></i></a></li>
            {% elseif route == 'blog_article_by_category_fr' %}
                <li><a href="{{ path('blog_article_by_categoryFr2', { 'page': pagination.previous_page, 'category':category.slugFr  }) }}"><i class="fas fa-angle-double-left"></i></a></li>
            {% elseif route == 'blog_article_by_categoryFr2' %}
                <li><a href="{{ path('blog_article_by_categoryFr2', { 'page': pagination.previous_page, 'category':category.slugFr  }) }}"><i class="fas fa-angle-double-left"></i></a></li>
            {% elseif route == 'blog_index_tag_fr' %}
                <li><a href="{{ path('blog_index_tag_fr2', { 'page': pagination.previous_page, 'category':category.slugFr  }) }}"><i class="fas fa-angle-double-left"></i></a></li>
            {% elseif route == 'blog_index_tag_fr2' %}
                <li><a href="{{ path('blog_index_tag_fr2', { 'page': pagination.previous_page, 'category':category.slugFr  }) }}"><i class="fas fa-angle-double-left"></i></a></li>
            {% endif %}
        {% else %}
            <li class="disabled"><a href="#"><i class="fas fa-angle-double-left"></i></a></li>
        {% endif %}

        {# Pages avec points de suspension si nécessaire #}
        {% for page in 1..pagination.total_pages %}
            {% if page == pagination.current_page %}
                <li class="active"><a href="#">{{ page|number_format(0) }}</a></li>
            {% elseif page <= 2 or page > pagination.total_pages - 2 or (page >= pagination.current_page - 1 and page <= pagination.current_page + 1) %}
                {% if route == 'blog_index_fr' %}
                    <li><a href="{{ path('blog_indexfr2', { 'page': page }) }}">{{ page|number_format(0) }}</a></li>
                {% elseif route == 'blog_indexfr2' %}
                    <li><a href="{{ path('blog_indexfr2', { 'page': page }) }}">{{ page|number_format(0) }}</a></li>
                {% elseif route == 'blog_article_by_category_fr' %}
                    <li><a href="{{ path('blog_article_by_categoryFr2', { 'page': page, 'category':category.slugFr }) }}">{{ page|number_format(0) }}</a></li>
                {% elseif route == 'blog_article_by_categoryFr2' %}
                    <li><a href="{{ path('blog_article_by_categoryFr2', { 'page': page, 'category':category.slugFr  }) }}">{{ page|number_format(0) }}</a></li>
                {% elseif route == 'blog_index_tag_fr' %}
                    <li><a href="{{ path('blog_index_tag_fr2', { 'page': page, 'tag':tag.slugFr }) }}">{{ page|number_format(0) }}</a></li>
                {% elseif route == 'blog_index_tag_fr2' %}
                    <li><a href="{{ path('blog_index_tag_fr2', { 'page': page, 'tag':tag.slugFr  }) }}">{{ page|number_format(0) }}</a></li>
                {% endif %}

            {% elseif page == 3 or page == pagination.total_pages - 2 %}
                <li><a href="#">...</a></li>
            {% endif %}
        {% endfor %}

        {# Bouton "Next" #}
        {% if pagination.has_next_page %}
            {% if route == 'blog_index_fr' %}
                <li><a href="{{ path('blog_indexfr2', { 'page': pagination.next_page }) }}"><i class="fas fa-angle-double-right"></i></a></li>
            {% elseif route == 'blog_indexfr2' %}
                <li><a href="{{ path('blog_indexfr2', { 'page': pagination.next_page }) }}"><i class="fas fa-angle-double-right"></i></a></li>
            {% elseif route == 'blog_article_by_category_fr' %}
                <li><a href="{{ path('blog_article_by_categoryFr2', { 'page': pagination.next_page, 'category':category.slugFr }) }}"><i class="fas fa-angle-double-right"></i></a></li>
            {% elseif route == 'blog_article_by_categoryFr2' %}
                <li><a href="{{ path('blog_article_by_categoryFr2', { 'page': pagination.next_page, 'category':category.slugFr }) }}"><i class="fas fa-angle-double-right"></i></a></li>
            {% elseif route == 'blog_index_tag_fr' %}
                <li><a href="{{ path('blog_index_tag_fr2', { 'page': pagination.next_page, 'tag':category.slugFr }) }}"><i class="fas fa-angle-double-right"></i></a></li>
            {% elseif route == 'blog_index_tag_fr2' %}
                <li><a href="{{ path('blog_index_tag_fr2', { 'page': pagination.next_page, 'tag':category.slugFr }) }}"><i class="fas fa-angle-double-right"></i></a></li>
            {% endif %}
        {% else %}
            <li class="disabled"><a href="#"><i class="fas fa-angle-double-right"></i></a></li>
        {% endif %}
    </ul>
</div>
